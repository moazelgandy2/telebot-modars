{
  "name": "telebot-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:bot": "npm --filter bot dev",
    "dev:web": "npm --filter web dev",
    "build": "npm -r build"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
