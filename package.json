{
  "name": "telebot-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:bot": "pnpm --filter bot dev",
    "dev:web": "pnpm --filter web dev",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
